# --- secrets (fill these in) ---
BOT_TOKEN=000000:REPLACE_WITH_BOTFATHER_TOKEN
ADMIN_TOKEN=REPLACE_WITH_A_LONG_RANDOM_STRING

# --- runtime config ---
DEV_MODE=false
SURVEY_STATE=open

# --- paths (data lives OUTSIDE the repo, on the persistent disk) ---
ITEMS_PATH=/home/melodiz/miniapp/data/items.json
DB_PATH=/home/melodiz/miniapp-data/app.sqlite3

# --- optional ---
# Salt for hashing rater ids; keep stable for the whole study.
# RID_HASH_SALT=REPLACE_WITH_A_RANDOM_STRING
# Reject initData older than N hours (default: off).
# INITDATA_MAX_AGE_HOURS=24
